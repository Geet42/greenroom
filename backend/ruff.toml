target-version = "py313"
line-length = 100

[lint]
select = ["E", "F", "W", "I"]
ignore = [
  "E501",  # line too long — handled by formatter
]

[lint.per-file-ignores]
"scripts/*" = ["E402"]
"data/*" = ["E402"]
