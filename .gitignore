node_modules/
dist/
.env
.env.local

__pycache__/
*.pyc
.venv/
venv/

.DS_Store
frontend/.env.production

# ephemeral scratch data used by one-off backend/scripts/*.py tools
# (downloaded datasets, intermediate candidate lists) — never source of truth
backend/scripts/_*.json

# k6 load-test throwaway-account tokens (live bearer tokens, ~1h lifetime) —
# regenerated locally via stress/fetch_stress_tokens.py, never source of truth
stress/tokens.json

# Grafana data source credentials filled in from the .yml.example templates —
# real Azure/Supabase secrets, never source of truth
infra/grafana/provisioning/datasources/azure-monitor.yml
infra/grafana/provisioning/datasources/supabase-postgres.yml
